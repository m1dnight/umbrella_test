name: Push

run-name: Push

on: [push]

jobs:
  compile:
    strategy:
      matrix:
        target: [dev, test, prod]
    uses: ./.github/workflows/compile.yaml
    with:
      mix-env: ${{ matrix.target }}
      # elixir_version: "1.17"
      # otp_version: "27.1"
