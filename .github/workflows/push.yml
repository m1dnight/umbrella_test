name: Push

run-name: Push

on: [push]

jobs:
  compile:
    strategy:
      matrix:
        target: [dev, test, prod]
    uses: m1dnight/umbrella_test/.github/compile.yaml
    with:
      mix-env: ${{ matrix.target }}
