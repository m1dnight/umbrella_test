name: Push

run-name: Push

on: [push]

jobs:
  compile:
    strategy:
      matrix:
        target: [dev, test, prod]
    uses: ./.github/workflows/runner_setup.yaml
    with:
      mix-env: ${{ matrix.target }}
